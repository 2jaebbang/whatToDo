import { css } from "styled-components";
import { defalutTheme } from "./theme";

export const headline1CSS = css`
  font-size: ${defalutTheme.fonts.size.xxxl};
  line-height: ${defalutTheme.fonts.lineheight.xxxl};
  font-weight: ${defalutTheme.fonts.weight.bold};
`;

export const headline2CSS = css`
  font-size: ${defalutTheme.fonts.size.xxl};
  line-height: ${defalutTheme.fonts.lineheight.xxl};
  font-weight: ${defalutTheme.fonts.weight.bold};
`;

export const headline3CSS = css`
  font-size: ${defalutTheme.fonts.size.xl};
  line-height: ${defalutTheme.fonts.lineheight.xl};
  font-weight: ${defalutTheme.fonts.weight.bold};
`;

export const headline4CSS = css`
  font-size: ${defalutTheme.fonts.size.lg};
  line-height: ${defalutTheme.fonts.lineheight.lg};
  font-weight: ${defalutTheme.fonts.weight.bold};
`;

export const body1CSS = css`
  font-size: ${defalutTheme.fonts.size.md};
  line-height: ${defalutTheme.fonts.lineheight.md};
  font-weight: ${defalutTheme.fonts.weight.bold};
`;

export const body2CSS = css`
  font-size: ${defalutTheme.fonts.size.sm};
  line-height: ${defalutTheme.fonts.lineheight.sm};
  font-weight: ${defalutTheme.fonts.weight.bold};
`;

export const captionCSS = css`
  font-size: ${defalutTheme.fonts.size.xs};
  line-height: ${defalutTheme.fonts.lineheight.xs};
  font-weight: ${defalutTheme.fonts.weight.normal};
`;
