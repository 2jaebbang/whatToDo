import styled from "styled-components";
import { theme } from "./theme";

export const headline1CSS = styled.div`
  font-size: ${theme.fonts.size.xxxl};
  line-height: ${theme.fonts.lineheight.xxxl};
  font-weight: ${theme.fonts.weight.bold};
`;

export const headline2CSS = styled.div`
  font-size: ${theme.fonts.size.xxl};
  line-height: ${theme.fonts.lineheight.xxl};
  font-weight: ${theme.fonts.weight.bold};
`;

export const headline3CSS = styled.div`
  font-size: ${theme.fonts.size.xl};
  line-height: ${theme.fonts.lineheight.xl};
  font-weight: ${theme.fonts.weight.bold};
`;

export const headline4CSS = styled.div`
  font-size: ${theme.fonts.size.lg};
  line-height: ${theme.fonts.lineheight.lg};
  font-weight: ${theme.fonts.weight.bold};
`;

export const body1CSS = styled.div`
  font-size: ${theme.fonts.size.md};
  line-height: ${theme.fonts.lineheight.md};
  font-weight: ${theme.fonts.weight.bold};
`;

export const body2CSS = styled.div`
  font-size: ${theme.fonts.size.sm};
  line-height: ${theme.fonts.lineheight.sm};
  font-weight: ${theme.fonts.weight.bold};
`;

export const captionCSS = styled.div`
  font-size: ${theme.fonts.size.xs};
  line-height: ${theme.fonts.lineheight.xs};
  font-weight: ${theme.fonts.weight.normal};
`;
